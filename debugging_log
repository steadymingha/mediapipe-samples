240920
**media pipe 3d issue
1. 스내치에서 몸을 다 폈는데 3d에서는 다리도 골반도 다 안핌(<180deg)
	이건 mmpose에서도 발견된이슈라 모델을 바꿔도 큰차이가 없을거같음
2. 추론값이 안나오는 프레임들이 있음(많음). 예를들어 1200 프레임을 넘기면 그안에서 추론값나오는 프레임만 카운트했을때 499개뿐
3. lpf를 넣어야할거같음. 값이 튀는데 특정 sigma 범위 안으로 들어오는걸 제거해버리면 룰 적용이 어려움
4. /data/rounds/241_002_dumbbell-snatch-left_009.mp4 가 0까지 안옴...... -> 해결. pose_world_landmarks 값 쓸것

240921
1. zero cross counting 에서 mediapipe 결과는 pose_world_landmarks를 써야함. pose_landmarks는 정규화된 값인데 봉에서 값도 튀고
rep 모션이 0을 넘지 않는 범위에서 찍히는 경우가 종종 있음. - lpf를 쓸 필요성이 약해졌으나 다른 예제에서 튀는값이 생기면 넣을것

- [x] z값 어느 정도 활용할수있는지? -> 활용하기 어렵다. 덤벨 스내치 몇 영상으로 계산했을때, 180도가 나와야하는 관절들(고관절, 무릎)각도가 대체로 150~160에 그침. 특정영상에서 170을 찍었으나 예외적인듯
- [ ] 룰적용(처음-중간-끝)구간을 어떻게 구분해야하는지? - 0 선을 기준으로 하면 될듯

2. 각도 계산 추가해서 보았으나 rep 룰의 판단 값으로 삼기에는 너무 부정확하다. 
3. 사피엔스로 마지막으로 돌려보고 접기로.
